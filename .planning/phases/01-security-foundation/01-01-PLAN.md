---
phase: 01-security-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [server.js]
autonomous: true
requirements: [SEC-01, SEC-02, SEC-03]
user_setup:
  - service: environment
    why: "Server will refuse to start without ADMIN_PASSWORD after SEC-01"
    env_vars:
      - name: ADMIN_PASSWORD
        source: "Set before starting: ADMIN_PASSWORD=your_password npm start"

must_haves:
  truths:
    - "Server exits immediately with error message if ADMIN_PASSWORD env var is not set"
    - "SVG files are rejected by the upload filter with error message"
    - "JSON data files remain valid after server crash during write (temp file pattern)"
  artifacts:
    - path: "server.js"
      provides: "SEC-01 password enforcement, SEC-02 SVG removal, SEC-03 atomic writes"
      contains: ["process.env.ADMIN_PASSWORD", "process.exit(1)", ".tmp", "renameSync"]
  key_links:
    - from: "server.js writeProducts()"
      to: "data/products.json"
      via: "temp file + atomic rename"
      pattern: "\\.tmp.*renameSync"
    - from: "server.js writeSiteData()"
      to: "data/site-data.json"
      via: "temp file + atomic rename"
      pattern: "\\.tmp.*renameSync"
---

<objective>
Устранение критических уязвимостей в server.js: принудительная загрузка пароля из env var (SEC-01), запрет SVG-загрузки (SEC-02), атомарная запись JSON-файлов (SEC-03).

Purpose: Эти три изменения устраняют самые критичные дыры — hardcoded пароль в исходном коде, stored XSS через SVG, и повреждение данных при сбое сервера.
Output: Обновлённый server.js с тремя исправлениями безопасности.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-foundation/01-RESEARCH.md
@server.js

<interfaces>
<!-- Current server.js structure executor needs to understand -->

From server.js — top-level constants (lines 1-17):
```javascript
const express = require('express');
const fs = require('fs');
const path = require('path');
const multer = require('multer');
const crypto = require('crypto');
const app = express();
const PORT = 3000;
const DATA_FILE = path.join(__dirname, 'data', 'products.json');
const SITE_DATA_FILE = path.join(__dirname, 'data', 'site-data.json');
const UPLOAD_DIR = path.join(__dirname, 'uploads');
const ADMIN_TOKEN = crypto.randomBytes(32).toString('hex');
```

From server.js — variables to be removed later (lines 21-23):
```javascript
var loginAttempts = {};
var LOGIN_LIMIT = 5;
var LOGIN_WINDOW = 15 * 60 * 1000;
```

From server.js — upload configuration (line 31):
```javascript
var ALLOWED_IMAGE_EXT = ['.jpg', '.jpeg', '.png', '.gif', '.webp', '.svg'];
```

From server.js — write functions (lines 75-77, 216-218):
```javascript
function writeProducts(products) {
    fs.writeFileSync(DATA_FILE, JSON.stringify(products, null, 2), 'utf-8');
}
function writeSiteData(data) {
    fs.writeFileSync(SITE_DATA_FILE, JSON.stringify(data, null, 2), 'utf-8');
}
```

From server.js — login handler password line (line 105):
```javascript
var adminPassword = process.env.ADMIN_PASSWORD || 'timur2024';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce ADMIN_PASSWORD env var (SEC-01)</name>
  <files>server.js</files>
  <read_first>
    - server.js (full file — understand current structure and login handler)
  </read_first>
  <action>
Replace line 105 in server.js:
```
var adminPassword = process.env.ADMIN_PASSWORD || 'timur2024';
```

With the following block (using `var`, no arrow functions, no template literals):
```javascript
var adminPassword = process.env.ADMIN_PASSWORD;
if (!adminPassword) {
    console.error('ОШИБКА: Задайте переменную окружения ADMIN_PASSWORD');
    console.error('Пример: ADMIN_PASSWORD=your_password npm start');
    process.exit(1);
}
```

This change is inside the `app.post('/api/login', ...)` handler. Only replace the single line 105 — do NOT touch the surrounding rate-limit code on lines 95-102 (that will be replaced in Plan 02 for SEC-08).

IMPORTANT: Use `var` for new declarations. Do NOT refactor existing `const` on lines 1-12 to `var` (per project convention, only new code uses `var`).
  </action>
  <verify>
    <automated>node -e "var s=require('fs').readFileSync('server.js','utf-8'); var checks=[s.indexOf('process.env.ADMIN_PASSWORD')>-1 && s.indexOf(\"'|| 'timur2024'\")===-1, s.indexOf('process.exit(1)')>-1, s.indexOf('ОШИБКА: Задайте переменную окружения ADMIN_PASSWORD')>-1]; console.log(checks.every(Boolean)?'PASS':'FAIL')"  </automated>
  </verify>
  <acceptance_criteria>
    - grep "|| 'timur2024'" server.js returns NO matches (hardcoded fallback removed)
    - grep "process.exit(1)" server.js returns exactly 1 match
    - grep "ОШИБКА: Задайте переменную окружения ADMIN_PASSWORD" server.js returns exactly 1 match
    - Running `node server.js` without ADMIN_PASSWORD env var exits with code 1 and prints the error message
    - Running `ADMIN_PASSWORD=test123 node server.js` starts successfully (then Ctrl+C to stop)
  </acceptance_criteria>
  <done>Server refuses to start without ADMIN_PASSWORD env var, prints clear Russian error message with usage example, exits with code 1. No hardcoded password fallback exists anywhere in the codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Remove SVG from uploads + atomic JSON writes (SEC-02, SEC-03)</name>
  <files>server.js</files>
  <read_first>
    - server.js (full file — verify current state after Task 1 changes)
  </read_first>
  <action>
Two independent changes in server.js:

**SEC-02 — Remove SVG from allowed extensions:**
On line 31, change:
```
var ALLOWED_IMAGE_EXT = ['.jpg', '.jpeg', '.png', '.gif', '.webp', '.svg'];
```
To:
```
var ALLOWED_IMAGE_EXT = ['.jpg', '.jpeg', '.png', '.gif', '.webp'];
```

This removes `.svg` from the whitelist. The existing `fileFilter` function (lines 35-43) already rejects files with extensions not in the array, so no other changes needed — SVG uploads will automatically be rejected with "Недопустимый тип файла: .svg".

**SEC-03 — Atomic writes for both data files:**

Replace the `writeProducts` function (currently lines 75-77):
```javascript
function writeProducts(products) {
    fs.writeFileSync(DATA_FILE, JSON.stringify(products, null, 2), 'utf-8');
}
```
With:
```javascript
function writeProducts(products) {
    var tmp = DATA_FILE + '.tmp';
    fs.writeFileSync(tmp, JSON.stringify(products, null, 2), 'utf-8');
    fs.renameSync(tmp, DATA_FILE);
}
```

Replace the `writeSiteData` function (currently lines 216-218):
```javascript
function writeSiteData(data) {
    fs.writeFileSync(SITE_DATA_FILE, JSON.stringify(data, null, 2), 'utf-8');
}
```
With:
```javascript
function writeSiteData(data) {
    var tmp = SITE_DATA_FILE + '.tmp';
    fs.writeFileSync(tmp, JSON.stringify(data, null, 2), 'utf-8');
    fs.renameSync(tmp, SITE_DATA_FILE);
}
```

Both functions now write to a `.tmp` file first, then atomically rename to the target file. On POSIX (Linux production), `fs.renameSync` is atomic. On Windows/NTFS it still provides crash protection (original file intact until rename completes).

IMPORTANT: Do NOT modify any other functions. Do NOT change `readProducts()` or `readSiteData()`. Only the two write functions change.
  </action>
  <verify>
    <automated>node -e "var s=require('fs').readFileSync('server.js','utf-8'); var c1=s.indexOf(\".svg\")===-1 || !s.match(/ALLOWED_IMAGE_EXT[^;]*\.svg/); var c2=(s.match(/\\.tmp/g)||[]).length>=2; var c3=(s.match(/renameSync/g)||[]).length>=2; console.log('SVG_removed:'+c1+' tmp_files:'+c2+' rename:'+c3, c1&&c2&&c3?'PASS':'FAIL')"  </automated>
  </verify>
  <acceptance_criteria>
    - grep "ALLOWED_IMAGE_EXT" server.js shows the array WITHOUT `.svg`
    - grep "\.tmp" server.js returns at least 2 matches (one per write function)
    - grep "renameSync" server.js returns at least 2 matches (one per write function)
    - grep "'\.svg'" server.js returns NO matches in the ALLOWED_IMAGE_EXT line
    - Server starts successfully with `ADMIN_PASSWORD=test123 node server.js`
    - Existing data files (products.json, site-data.json) remain readable and valid JSON
  </acceptance_criteria>
  <done>SVG files rejected on upload. Both writeProducts() and writeSiteData() use temp-file-then-rename pattern. Server starts and serves data correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted input crosses into server via HTTP requests |
| Admin → API | Authenticated but input still untrusted — must validate all fields |
| Server → Filesystem | Data writes to JSON files — crash-safe writes critical |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | server.js line 105 | mitigate | SEC-01: Remove hardcoded password fallback, require env var. Server exits without it. |
| T-01-02 | Tampering / Elevation | Upload fileFilter | mitigate | SEC-02: Remove SVG from allowed extensions to prevent stored XSS via embedded scripts. |
| T-01-03 | Denial of Service | writeProducts/writeSiteData | mitigate | SEC-03: Atomic writes via temp file + rename prevent data corruption on server crash. |
</threat_model>

<verification>
1. Start server WITHOUT ADMIN_PASSWORD: `node server.js` → must exit with code 1 and Russian error
2. Start server WITH ADMIN_PASSWORD: `ADMIN_PASSWORD=test123 node server.js` → must start on port 3000
3. Verify SVG rejection: `curl -X POST -F "images=@test.svg" http://localhost:3000/api/products` → must return 400
4. Verify atomic writes: Create/edit a product via admin, check data/products.json is valid JSON
5. Verify no .tmp files remain after successful write: `ls data/*.tmp` → empty
</verification>

<success_criteria>
- Server exits with code 1 and Russian error message when ADMIN_PASSWORD is not set
- No hardcoded password (`'timur2024'`) exists anywhere in server.js
- `.svg` is absent from ALLOWED_IMAGE_EXT array
- Both write functions contain `.tmp` + `renameSync` pattern
- Server starts and serves API correctly with valid ADMIN_PASSWORD
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-foundation/01-01-SUMMARY.md`
</output>
